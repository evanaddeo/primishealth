/**
 * Check-in feature barrel (CU-074).
 *
 * The daily check-in screen plus its pure model helpers. The route file
 * (app/check-in.tsx) imports the screen from here.
 */

export { CheckInScreen } from './CheckInScreen';
export * from './checkinModel';
