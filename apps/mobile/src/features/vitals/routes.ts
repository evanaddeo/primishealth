/**
 * Vitals route constant (CU-067).
 *
 * The Vitals detail is a stack drill-down route (`app/vitals`) reached from
 * Recovery / Home, not a bottom tab (UX-NAV-002 / plan assumption A2).
 */

export const VITALS_ROUTE = '/vitals' as const;
