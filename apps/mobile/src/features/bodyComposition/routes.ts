/**
 * Body Composition route constant (CU-067).
 *
 * The Body Composition detail is a stack drill-down route (`app/body-composition`)
 * reached from the Vitals screen, not a bottom tab (UX-NAV-002 / plan A2).
 */

export const BODY_COMPOSITION_ROUTE = '/body-composition' as const;
