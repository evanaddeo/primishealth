/**
 * Daily check-in route (CU-074).
 *
 * Navigation target for the Home "Check in" entry point. The screen — optional
 * energy/mood/stress/soreness selectors plus notes — lives in the checkin
 * feature.
 *
 * @see apps/mobile/src/features/checkin/CheckInScreen.tsx
 */

import { CheckInScreen } from '../src/features/checkin';

export default CheckInScreen;
