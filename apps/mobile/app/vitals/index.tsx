/**
 * Vitals detail route (CU-067).
 *
 * Stack drill-down target reached from the Recovery screen (and, later, Home).
 * The screen itself — source/freshness header, vitals vs baseline, trends, and a
 * link to Body Composition — lives in the vitals feature.
 *
 * @see apps/mobile/src/features/vitals/VitalsScreen.tsx
 */

import { VitalsScreen } from '../../src/features/vitals';

export default VitalsScreen;
