/**
 * Body Composition detail route (CU-067).
 *
 * Stack drill-down target reached from the Vitals screen. The trend-first screen
 * — weight, body fat, and lean-mass trends with source/freshness — lives in the
 * bodyComposition feature.
 *
 * @see apps/mobile/src/features/bodyComposition/BodyCompositionScreen.tsx
 */

import { BodyCompositionScreen } from '../../src/features/bodyComposition';

export default BodyCompositionScreen;
