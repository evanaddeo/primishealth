/**
 * Bedtime Planner route (CU-064).
 *
 * The navigation target for the Sleep screen's Bedtime Planner entry point
 * (UX-SLEEP-004) and the Home bedtime widget. The screen itself — wake-time
 * selection and ranked bedtime windows — lives in the bedtime feature.
 *
 * @see apps/mobile/src/features/bedtime/BedtimePlannerScreen.tsx
 */

import { BedtimePlannerScreen } from '../../src/features/bedtime';

export default BedtimePlannerScreen;
