/**
 * @primis/workers — provider sync worker service.
 *
 * This package contains:
 *   - Provider connector interfaces and test doubles
 *   - Raw payload archive abstraction (CU-036)
 *   - Google Health OAuth skeleton and API client (CU-037, CU-039)
 *   - Normalization pipeline (CU-041–043)
 *   - Idempotent record writer (CU-044)
 *   - Sync job runner (CU-045)
 *
 * Exports grow as each phase CU is implemented. This placeholder ensures the
 * package resolves correctly for downstream consumers before any logic is added.
 */

export {};
