# Dependencies
node_modules/

# Build outputs
dist/
build/

# TypeScript incremental build info
*.tsbuildinfo

# Test coverage
coverage/

# Environment files (never commit — use .env.example for templates)
.env
.env.local
.env.*.local
# Allow .env.example
!.env.example

# macOS metadata
.DS_Store
.AppleDouble
.LSOverride

# Editor directories
.idea/
.vscode/settings.json

# pnpm
.pnpm-store/

# Expo / React Native
.expo/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# AWS CDK
cdk.out/
cdk.context.json

# Local raw payload archive (CU-036 / E-DRIFT-006)
# Generated by LocalRawPayloadArchive during development and CI fixture generation.
# Never commit — may contain unredacted provider data in development scenarios.
database/fixtures/.local-dev-archive/

# Google Health spike script output (CU-040)
# Report files and any raw payload dumps from live mode runs.
# Never commit — may contain live API response data.
scripts/google-health-spike/output/
