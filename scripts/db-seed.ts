#!/usr/bin/env tsx
/* eslint-disable no-console */
/**
 * CLI entrypoint for `pnpm db:seed`.
 *
 * CU-026 stub — no seed scripts exist yet.
 * CU-029 will replace this stub when the metric_definitions seed is implemented.
 *
 * Usage:
 *   pnpm db:seed
 */

console.log('[db:seed] No seed scripts registered yet. (Stub — populated in CU-029.)');
process.exit(0);
