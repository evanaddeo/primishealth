/**
 * Correlation engine barrel (CU-094).
 *
 * Pure, deterministic group-difference association analysis — types, fixed
 * v1 factor/outcome allowlist, input preparation, and the engine itself.
 * See Scoring Spec §22 and the Phase K plan §9.
 */

export * from './correlationTypes.js';
export * from './groupDifference.js';
export * from './correlationEngine.js';
