/**
 * Placeholder smoke test — confirms the Vitest runner is wired up correctly
 * for the @primis/config package.
 *
 * This file will be replaced by real tests in CU-007 when the Zod env
 * contract (`env.ts`) is implemented.
 */
import { describe, expect, it } from 'vitest';

describe('placeholder', () => {
  it('the test runner executes', () => {
    expect(true).toBe(true);
  });
});
